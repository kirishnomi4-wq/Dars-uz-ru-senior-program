import React from 'react'
import ReactDOM from 'react-dom/client'
import EtalonDemoApp from './EtalonDemoApp.jsx'

ReactDOM.createRoot(document.getElementById('root')).render(
  <React.StrictMode>
    <EtalonDemoApp />
  </React.StrictMode>,
)
