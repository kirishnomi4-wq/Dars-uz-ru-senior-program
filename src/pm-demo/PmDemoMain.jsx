import React from 'react'
import ReactDOM from 'react-dom/client'
import PmDemoApp from './PmDemoApp.jsx'

ReactDOM.createRoot(document.getElementById('root')).render(
  <React.StrictMode>
    <PmDemoApp />
  </React.StrictMode>,
)
